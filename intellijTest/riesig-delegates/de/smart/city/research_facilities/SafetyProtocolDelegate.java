package de.smart.city.research_facilities;

import java.time.LocalDate;

/**
 * Delegate von SafetyProtocol * Wird nur initial generiert.
 * Methoden müssen implementiert werden.
 **/
public class SafetyProtocolDelegate {
    static {
        SafetyProtocol.delegate = new SafetyProtocolDelegate();
    }

}