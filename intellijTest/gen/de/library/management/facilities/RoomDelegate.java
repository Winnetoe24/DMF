package de.library.management.facilities;

/**
 * Delegate von Room * Wird nur initial generiert.
 * Methoden müssen implementiert werden.
 **/
public class RoomDelegate {
    static {
        Room.delegate = new RoomDelegate();
    }

}