package de.library.management.verification;

/**
 * Delegate von University * Wird nur initial generiert.
 * Methoden müssen implementiert werden.
 **/
public class UniversityDelegate {
    static {
        University.delegate = new UniversityDelegate();
    }

}