create table Aufgabe (
    id INTEGER,
    frage VARCHAR(255),
    antwort VARCHAR(255),
    beispiel_inhalt VARCHAR(255),
    beispiel_typ_ID INTEGER,
    beispiel_i INTEGER,
    PRIMARY KEY(id)
);


