dmf 1.0.0
model "import" version 1.0.0

package domain {
    // Kommentar aus anderer Datei
    struct DomainElement {
        arg double i;
    }

    entity TestE {

        arg int i;

        identifier(i);
    }
}